#!/bin/bash

testname=${1:-$(date +test%Y%m%d%H%M%S)}
interval=${2:-60}

pactmp=$(dirname ${TOOL_ROOT})

echo "testname:" ${testname}
echo "interval:" ${interval}

nohup paracmd -t ${pactmp} -n 2 -d pacmon -l \
"psar -i ${interval} -t ${testname}" \
"psps -i ${interval} -t ${testname}" \
"" &
